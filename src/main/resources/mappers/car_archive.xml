<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.car.dao.Car_ArchiveDao">
    <select id="listAll" resultType= "com.example.car.entity.Car_Archive">
        SELECT * FROM car_Archive
    </select>
    <insert id="insert" parameterType="com.example.car.entity.Car_run"/>
</mapper>